<div class="max-w-2xl mx-auto p-6">
  <h1 class="text-2xl font-bold mb-6">Advance Week</h1>

  <%= turbo_frame_tag "week-step" do %>
    <%= render "week_advances/steps/#{@step}" %>
  <% end %>
</div>
